export interface Article {
  article: string;
  views_ceil: number;
  rank: number;
}

export interface Option {
  value: number;
  label: number;
}

export interface Country {
  name: string;
  code: string;
}
